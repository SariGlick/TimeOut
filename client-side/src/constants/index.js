
  const TITLES = {
    SELECT_EMAIL_FREQUENCY: 'Select date format',
  };

  const LABELS = {
    SEND_FORMAT_DATE:'Send Format Date'
  };

  const DATE_FORMATS = [
    { value: 'yyyy-MM-dd', label: 'yyyy-MM-dd' },
    { value: 'dd-MM-yyyy', label: 'dd-MM-yyyy' },
    { value: 'MM-dd-yyyy', label: 'MM-dd-yyyy' }
  ];

  const CONSTANTS = {
    LABELS,
    DATE_FORMATS,
    TITLES
  };
  
  export default CONSTANTS;